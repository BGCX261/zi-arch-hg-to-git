{% extends "::base.html.twig" %}
{% block body %}
<h1>License edit</h1>

<form action="{{ path('license_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }} class="well">
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('license') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('license_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}