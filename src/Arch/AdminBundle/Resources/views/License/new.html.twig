{% extends "::base.html.twig" %}
{% block body %}
<h1>License creation</h1>

<form action="{{ path('license_create') }}" method="post" {{ form_enctype(form) }} class="well">
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('license') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}